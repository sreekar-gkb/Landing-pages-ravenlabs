export default function TestPage() {
  return (
    <main style={{ padding: 60, fontFamily: "sans-serif" }}>
      <h1>Skill Verification Test 2 — post-fix</h1>
      <p>Confirms deploy-campaign now actually triggers a real Vercel deployment.</p>
    </main>
  );
}
